/**
 * Auth route group layout — applies the centered card shell.
 */

import AuthLayout from '@/src/layouts/AuthLayout';

export default function AuthGroupLayout({ children }) {
  return <AuthLayout>{children}</AuthLayout>;
}
